# Acceptance report: 5.0.0

Datum ověření: **2026-09-10**.

## Automatické testy

- `./test/unit.sh` — **292/292**.
- `./test/run.sh` — **53/53**.
- `git diff --check` — bez nálezu.

## Čistý projekt: live smoke test

Ověření proběhlo nad třemi dočasnými, nově inicializovanými Git repozitáři,
se samostatným dočasným `HOME` a konfigurací. Použit byl zdrojový launcher a
Python 3.11; smoke test se stackem nepoužíval `--no-grepai` ani
`--no-gitnexus`.

| Agent | `--apply` | `--status --json` | `--refresh` | `--remove --apply` |
| --- | ---: | ---: | ---: | ---: |
| `claude` | 0 | 0 | 0 | 0 |
| `codex` | 0 | 0 | 0 | 0 |
| `both` | 0 | 0 | 0 | 0 |

Samostatně `--install-deps --no-install-deps` našel všech devět nástrojů a
vrátil **0**. Tento režim se spouští před projektovým dispatch a nepoužívá
`agent_target`, takže jediný běh pokrývá `claude`, `codex` i `both`. Tímto
během se nic neinstalovalo.

## Behaviorální brána

Výsledek fresh headless relací a klasifikace čtecích operací je zapsaný v
[měřicím plánu](../plans/code-context-toolchain.md), zejména v části 5:

- referenční stav: **1/9 = 11 %**;
- silná brána C, běh 3, bez zmínky skillu v zadání: **3/3 = 100 %**;
- kritérium vydání: **nejméně 80 %** odvozených rozsahů nebo celých souborů
  pod přibližně 100 řádků po ověření velikosti.

Pokus o nové opakování v této relaci byl proveden nad aktuálním checkoutem
bez zmínky skillu, ale lokální Claude CLI skončilo ještě před promptem:
`Not logged in · Please run /login`. Tento pokus proto není započítán do
metriky a neoznačuje se jako úspěšné měření.

## Graft

Graft nebyl odebrán. Issue podmiňuje jeho odinstalaci tím, zda si ho uživatel
nechce ponechat; tato preference nebyla zadána a odinstalace by byla změnou
mimo repozitář.
