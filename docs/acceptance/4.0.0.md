# Acceptance report — agent-code-intel 4.0.0

Status: **candidate switched; mandatory matrix and live smoke green; day-1 and
day-2 complete; the third live session is recorded, while the distinct-day and
user-session restart gates remain pending.** This
report was opened by issue #49 and grew one implementation slice at a time
(issues #50–#61). It is green only when every mandatory scenario has run and
every inventoried invariant traces to a named scenario — see [issue #43
§6](https://github.com/eduardtomasek/agent-code-intel/issues/43).

The historical `code-intel-init` 2.4.1 audit
([`docs/acceptance/code-intel-init-2.4.1.md`](code-intel-init-2.4.1.md)) is a
separate document and is not rewritten here. Issue #57 moved it under its
versioned acceptance name alongside the README/CHANGELOG work.

---

## 1. Reference and environment

| | |
|---|---|
| Behavioural reference | `9406cce` — `git show 9406cce:agent-code-intel` (2233 lines, `VERSION="3.0.0"`) |
| Candidate | active `agent-code-intel` + `agent_code_intel/` (issue #50: runtime gate + CLI parser; #51: config + identity; #52: install / upgrade; #53: status table + JSON status, the external-stack probes in `integrations.py`, the registry reader and `.grepai/config.yaml` checks in `project.py`; #54: the `refresh` mode — the lighter preflight, the re-index with its one `--force` retry, and the audit that still runs after a failed re-index; #56: safe `remove` planning/apply teardown with ownership guards; #57: pre-cutover evidence and documentation; #58: active-entrypoint switch). The source version is `4.0.0`; the exact switch SHA is recorded below after the commit. |
| Reference acquisition | `test/lib/reference.sh` → temp executable, no second long-lived copy in the working tree (issue #48, decision 68) |
| Mandatory Python | 3.11.16 (`~/.local/bin/python3.11`) — the contract version, `tomllib` (issue #48, testing decision 1–4). `test/lib/isolated_path.sh` demands a `3.11.x` interpreter by default and **errors** if none is found; it never silently drops to a newer minor. |
| Supplementary Pythons | 3.13.x / 3.14.x present on the machine — extra evidence, not a CI matrix. Run via `ACI_PYTHON=python3.14 test/reference-suite.sh`. |
| Rejected Python | `/usr/bin/python3` = 3.9.6 — the runtime gate must reject it with exit 1, no traceback (proven in a later slice) |
| Isolated PATH | `<pybin>:/usr/bin:/bin` where `<pybin>` holds only a `python3` symlink; no grepai / gitnexus / ollama / node / claude / codex (issue #43 §3) — built by `test/lib/isolated_path.sh` |
| OS / shell / git | macOS 26.6.2 · system bash 3.2.57 · git 2.50.1 |

---

## 2. Evidence layers (issue #43 §2)

| Layer | What it proves | State after issue #61 |
|---|---|---|
| 1. Existing black-box suite | `test/run.sh`, 53 `test_*`, expectations **not** rewritten for the port; runs against reference and candidate | **green against the reference and candidate** under an isolated Python 3.11 PATH — `test/reference-suite.sh` and `test/run.sh`: parser/early-exit, install, status, refresh, preview and all five `test_remove_*` scenarios pass, 53/53. The candidate run uses the active `agent-code-intel` entrypoint. |
| 2. `unittest` unit tests | pure logic: parser/early-exit, TOML, ENV-harvester result, identity precedence, foreign-output parsers, domain→finding, managed-file transforms, import purity, three error paths | **green** — `test/unit.sh`, 243 tests under Python 3.11.16: parser matrix, early exit, runtime gate (run under real 3.9.6), import purity for all six modules, launcher/template parity, (issue #51) the config loader (`test_config.py`), identity resolution (`test_project.py`), the shared seams (`test_seams.py`), (issue #52) install / upgrade (`test_install.py`), (issue #53) the status mode, (issue #54) the refresh mode, (issue #55) preview/apply orchestration, (issue #56) remove ordering, ownership and remote-failure tolerance (`test_remove.py`), (issue #57) the final pipeline, alias, analyze-output and old-Node evidence, and (issue #59) the supported Claude MCP lookup argv. |
| 3. Hermetic differential harness | both implementations over separated fixtures, whole observable result compared | **engine in place** — `test/differential.sh` (with a real `toml` lane; a per-scenario `rm -rf` of each run's tree); self-check green (§4). Active `agent-code-intel`, both lanes: 7 parser scenarios; (issue #51) config/identity scenarios; (issue #52) install scenarios; (issue #53) status scenarios; (issue #54) refresh scenarios; and (issue #56) `remove-dry-run` + `remove-apply` — ENV and TOML lanes both run against the switched candidate, with only release-version/TOML metadata divergences declared. |
| 4. Opt-in live integration | real stack, prefixed throwaway identities, healthy lifecycle | **three live sessions green for the executed lifecycle** on three real repositories with real Qdrant/Ollama/GrepAI/GitNexus and both agents; the third session proves installed-CLI independence and the unchanged dashboard smoke, while a distinct calendar day and a user-session/machine restart remain unclaimed |

### How to run

```
test/reference-suite.sh                      # layer 1 against the reference
test/unit.sh                                  # layer 2 (Python 3.11.x; ACI_PYTHON overrides)
test/differential.sh                         # layer 3, env lane, self-check
ACI_CANDIDATE=./agent-code-intel test/differential.sh <scenario>...      # against the candidate
ACI_LANES="env toml" ACI_CANDIDATE=./agent-code-intel test/differential.sh cli-version cli-unknown-flag cli-early-exit cli-flags-matrix cli-parse-error-exit cli-workspace-twice runtime-gate config-both-conflict config-toml-errors config-toml-bad-type config-toml-unparsable config-unset-name config-version-not-overridable config-array-empty config-env-output identity-code-intel-broken identity-refresh-outside-git install-clean install-upgrade-v3 install-upgrade-v2 install-upgrade-v4 install-foreign-lib install-symlink-lib status-json-drift status-text-drift status-all-registry status-all-empty json-broken-project refresh-no-code-intel refresh-skip-both refresh-skip-grepai refresh-preflight-blocked refresh-skip-gitnexus init-preflight-blocked remove-dry-run remove-apply
```

The install and status scenarios also pass under the default `env` lane alone.
The black-box `test_install_*`, (issue #53) `test_status*` /
`test_*code_intel*` / `test_workspace_*` / `test_*_workspace_*` /
`test_grepai_name_conflict*` / `test_path_flag_overrides_cwd` /
`test_suite_does_not_touch_real_home`, (issue #54) `test_refresh_*` and (issue
#56) `test_remove_*` tests (`test/run.sh`) pass against the candidate under an
isolated 3.11 PATH — 53 of 53:

```
WORK=$(mktemp -d); PB="$WORK/pb"; mkdir -p "$PB"; ln -sf "$(command -v python3.11)" "$PB/python3"
TOOL=$PWD/agent-code-intel EXPECTED_NAME=agent-code-intel ACI_TEST_PATH="$PB:/usr/bin:/bin" \
  test/run.sh
```

Naming a scenario is deliberate while the port is partial: a bare
`ACI_CANDIDATE=… test/differential.sh` also runs mode scenarios that are not yet
converted; those remain unimplemented until their owning slices land (issue
#48, decision 70).

---

## 3. Differential harness contract (issue #43 §4)

- **Two lanes per scenario:** `env` (candidate + `defaults.env` vs reference + the
  same `defaults.env`) and `toml` (candidate + `defaults.toml` vs reference + an
  equivalent `defaults.env`). A mutating scenario uses up to four independent
  fixtures. The lane config comes from `scenario_env_config` (env lane, both
  implementations; reference in the toml lane) and `scenario_toml_config` (the
  candidate in the toml lane); issue #51's error-path scenarios instead write
  the config file directly in `scenario_setup` so the divergence they assert is
  identical in both lanes.
- Each `run_one` starts by `rm -rf`-ing its `<lane>/<impl>` tree, so selecting
  several scenarios in one invocation cannot leak one scenario's config or
  registry into the next.
- **Separated per implementation per lane:** `HOME`, project, registry, and a
  distinct workspace name (each `run_one` passes the scenario a `<impl>-<lane>`
  tag; the qdrant collection name is `workspace_<ws>`, so it separates with the
  workspace). No implementation runs over state another run touched.
- **Compared separately:** exit code · stdout (raw, normalized — order and
  whitespace **not** normalized) · stderr · structural JSON (types, conditional
  fields, key sets — a second gate on top of the raw stdout diff, order-insensitive) ·
  file manifest (relative path, type, octal mode, hash of normalized content;
  `.git` excluded) · declared external effects (`scenario_observe_effects`; the
  hermetic seed scenarios declare none — stub-backed effect scenarios arrive
  with the integration slices, issue #42).
- **Normalized only:** the canonical fixture root → `{FIXTURE}`, the canonical
  home root → `{HOME}`, the per-run workspace tag → `{TAG}`, and a **format-gated**
  `meta.generated_at` → `{GENERATED_AT}` (a value that fails the ISO-8601-`Z`
  check is left as-is and shows as a difference). Not order, not whitespace, not
  exit, not health classification, not the spelling of a registered path.
- **Known divergences are positive scenarios** with an exact local diff, never a
  global ignore list (§5). A scenario declares one with
  `scenario_expected_divergence` (echo the dimensions allowed — **and
  required** — to differ). A dimension outside the list that differs still
  fails; a listed dimension that does *not* differ also fails (a stale
  divergence claim). Reference-vs-reference, such a scenario is
  `unimplemented` — it needs a real candidate to have anything to diverge.
  Since issue #53 the hook is **lane-aware**: it is called once per lane with
  the lane name (`env` / `toml`) as `$1`, so a divergence that exists in only
  one lane can be declared for that lane alone. `--status --json` uses this —
  the port's `meta.config_file` matches the reference byte-for-byte with a
  `defaults.env` (env lane) but names `defaults.toml` under the toml lane
  (issue #37 §7). A scenario that ignores `$1` keeps the old
  whole-scenario behaviour; the self-check gate asks the hook about the `env`
  lane, so a toml-lane-only divergence still self-checks in the env lane.
- A scenario may run under a **non-default PATH** via `scenario_path_override`
  — used only by `runtime-gate`, to reach the machine's sub-3.11
  `/usr/bin/python3`. Still hermetic: no grepai / gitnexus / ollama / node /
  claude / codex.
- `PYTHONDONTWRITEBYTECODE=1` is set for every run so a Python candidate's
  bytecode caches never enter the file manifest.
- **`toml` lane in the reference-vs-reference self-check reports
  `unimplemented`** — the reference has no TOML reader, so there is nothing to
compare; against the active `agent-code-intel` candidate the lane runs for real
  (issue #46 §3, issue #51).

---

## 4. Harness self-check scenarios (issue #49)

These run the reference against **itself** over separated fixtures. They prove
the harness mechanics — fixture isolation, every comparison dimension, the
declared normalizations — **not** any port. As of issue #50 a candidate exists
(`agent-code-intel`); `cli-version` and `cli-unknown-flag` now also run green
against it and carry their parser rows below.

| Scenario | Mode exercised | Dimensions exercised | Result |
|---|---|---|---|
| `cli-version` | `--version` | exit 0, stdout, stderr | zero diff |
| `cli-unknown-flag` | `--definitely-not-a-flag` | **exit 1**, stderr (`[ERROR: …]`) — proves a failing tool's exit code and stderr are captured, not just the happy path | zero diff |
| `status-json-drift` | `--status --all --json` over a registered project, no stack | structural JSON + raw stdout, `generated_at` gating, file manifest (read-only); env lane in the self-check | zero diff |
| `status-text-drift` | `--status` on a set-up project, no stack | `hr` byte-length rule, `row` format, exit 2, the drift-report tail | zero diff |
| `status-all-registry` | `--status --all` over 4 rows (drift / BROKEN / GONE / ABSENT) | the registry loop, the row vocabulary, one broken row not hiding the rest | zero diff |
| `status-all-empty` | `--status --all`, empty registry | `registry is empty (<path>)`, exit 0 | zero diff |
| `remove-dry-run` | `--remove` with `.code-intel` + pristine `refresh-intel.sh` | exit, stdout/stderr, unchanged manifest | zero diff |
| `remove-apply` | `--remove --apply` with `.code-intel` + pristine `refresh-intel.sh` | separated mutating fixtures + per-run workspace tag, manifest diff (deletions), effects seam | zero diff |
| `config-array-empty` | `--version` with `EXTRA_IGNORES=()` in `defaults.env` | scenario-written config in `$HOME`, per-scenario fixture wipe (`rm -rf "$base"`) | zero diff |
| `config-env-output` | `--version` with a `defaults.env` that writes to stdout and stderr | the harvester inherits both streams (issue #38 §2.10) — the file's stdout line lands before the banner, its stderr line on stderr | zero diff |
| `identity-code-intel-broken` | init preview with a malformed `.code-intel` in a git repo | fixture-written `.code-intel`, canonical-path normalization in stderr | zero diff |
| `refresh-skip-both` | `--refresh --no-grepai --no-gitnexus` on a set-up project | the preflight + refresh headers, the `hr` rule with an em dash in the path, exit 0, the `Code intelligence is fresh.` tail | zero diff |
| `refresh-preflight-blocked` | `--refresh`, no stack, qdrant pinned at a closed port | the `MISSING` row vocabulary on stdout, the multi-line `[ERROR: …]` block on stderr, exit 1, read-only manifest | zero diff |

Negative controls (checked, not committed): a candidate with `VERSION` bumped
fails `cli-version` on `stdout` and `status-json-drift` on structural JSON
(`script_version`); a candidate that swallows the unknown-flag error fails
`cli-unknown-flag` on the `exit` dimension. The comparator is not a no-op.

---

## 5. Approved divergences (issue #35) — each becomes its own positive scenario

| ID | Divergence | Class | Owning slice |
|---|---|---|---|
| DEV-1 | shebang `python3` + inline runtime gate `>= 3.11`; no self-re-exec | [S] | #50 — **done**: scenario `runtime-gate` (exit/stdout/stderr diverge under 3.9.6), unit `test_runtime_gate.py`. Message: `[ERROR: agent-code-intel requires Python 3.11 or newer (found <M>.<m>.<p>)]` |
| DEV-2 | parse-error exit stays **1**, not argparse's 2 | [Z] | #50 — **done**: the port hand-rolls the loop, no argparse; `cli-parse-error-exit`, `cli-unknown-flag`, RT-12 |
| DEV-3 | `not inside a git repository` message gains the real path | [S] | #51 / #54 — **done**: the message is produced by `project.resolve_project`. Scenario `identity-refresh-outside-git` (stderr diverges: the reference blanks `ROOT` in `ROOT="$(git …)"` on failure and interpolates the empty value; the port carries the real directory) now runs against the candidate (#54). The `--path`-explicit refresh path is B-covered against the candidate (`test_refresh_path_flag_is_explicit_override`). |
| DEV-4 | preview banner stops claiming "no changes will be made" (behaviour kept) | text [S] | #55 |
| DEV-5 | `--status --json` ignores `--all`, always exits 0 | [Z] | #53 — **done**: `_status_json` enumerates the whole registry regardless of `status_all` and `run_status` returns 0 after a successful build. D `status-json-drift` (`--status --all --json`, exit 0 with every probe in drift), `json-broken-project`; U `test_status.JsonDocument` (`test_all_is_ignored_registry_always_enumerated`, `_doc` asserts exit 0). |
| DEV-6 | `--install` had no python3 check → bare 127; structurally impossible in Python | [N] conscious loss | #52 — **recorded**: the launcher's runtime gate rejects a sub-3.11 interpreter before the package imports, so `--install` can never reach a bare 127; there is no `python3` check to omit. No scenario. |
| DEV-7 | silent mode exclusivity (`--status --remove` → remove wins) | [Z] | #50 — parser done (last mode-switch wins, no exclusivity check: unit `test_last_mode_switch_wins…`); now also observable through the status header when status wins (`--remove --status`), fully proven once `--remove` lands (#56) |
| DEV-8 | `--json` suppresses the header in `--refresh` / `--remove` too | [Z] | #53 / #54 — **done for status and refresh**: `run_status` and `run_refresh` print the `Project:` / `Workspace:` / `Agents:` header only when `not as_json`. U `test_status.TextTable.test_header_printed_without_json_and_suppressed_with_json`, `test_refresh.Shape.test_header_suppressed_by_json`; D `status-json-drift` vs `status-text-drift`. `--remove` inherits the same rule when it lands (#56). |
| DEV-9 | `--help` / `--version` short-circuit everything to their right | [Z] | #50 — **done**: faithful left-to-right loop, no argv pre-scan needed; `cli-early-exit`, unit `test_early_flag_short_circuits…` (incl. `--path --help` consuming `--help` as the path arg) |
| DEV-10 | `WS` sanitized, `PROJ_NAME` not | [Z] | #51 — **done** (U): `test_project.test_absent_falls_back_to_the_basename_slug` (`My.Repo` → WS `my-repo`, PROJ_NAME `My.Repo`). #53 makes the `Workspace:` banner observable (black-box `test_workspace_derived_from_basename` passes against the candidate); a dedicated D scenario for the raw `PROJ_NAME` in a JSON `name` field vs the sanitized `workspace` waits on a stack-backed alias fixture (hermetic gap). |
| DEV-11 | *(deferred to #38 → became DEV-13)* | — | — |
| DEV-12 | `stale .grepai/index.gob` reported but never raises drift | [Z] | #53 — **done**: `_status_one` prints the `stale .grepai/index.gob present (rm it)` row without touching `bad`, and `_json_projects` sets `gob_leftover` without touching `bad`. U `test_status.TextTable.test_stale_gob_is_reported_but_never_the_cause_of_drift` (an otherwise-healthy project stays `ok`). |
| DEV-13 | `VERSION=` in `defaults.env` no longer overrides `--version` / banner | narrowing, accepted | #51/#58 — **done**: scenario `config-version-not-overridable` (stdout diverges `9.9.9` vs `4.0.0`); `VERSION` is not among the 16 transferred names. |
| DEV-14 | empty config array = empty list (was a silent exit 1) | approved fix | #51 — **done**: scenario `config-array-empty` (no regression under `--version`); unit `test_config` covers `EXTRA_IGNORES=()` (ENV) and `extra_ignores = []` (TOML) → empty list. |
| DEV-15 | the qdrant health probes are time-bounded (`curl --connect-timeout 3 --max-time 5`; a 3 s socket connect timeout) where the reference's `curl` / `/dev/tcp` have none | approved at review | #53 — **done**: `integrations.Stack.qdrant_http_ok` / `qdrant_grpc_ok`. The reference's timeout-less `curl` hangs `--status --json` forever against a qdrant that accepts the connection but never answers HTTP; `--status` must stay usable on an unhealthy machine (issue #53 AC 4). Refused / healthy ports behave identically and fast, so no scenario diverges under a normal-or-absent qdrant. U `test_integrations.StackProbes.test_qdrant_probes_are_time_bounded`. |

Text exceptions (issue #35 §3, extended to 9): TXT-1 install command form ·
TXT-2 runtime-gate message · TXT-3 preview banner · TXT-4 git-repo message
(**live**, #51: `not inside a git repository: <dir> — pass --path, or run
--apply in a git repo first`) · TXT-5 anything TOML forces (**live**, #51:
`<conf-dir>: both defaults.env and defaults.toml exist; keep one` ·
`<toml>: unknown key '<k>'` · `<toml>: key '<k>' must be an integer, got
<type>` · `<toml>: <tomllib message with line/column>`. The type-mismatch line
generalises issue #37 §7's single `integer` example to the other typed slots
with the identical template — `must be a string, got <type>` and `must be an
array of strings, got <type>`, `<type>` ∈ string/integer/float/boolean/array/table
— open for a wording ruling at review) · TXT-6 missing-`bash` message
(**live**, #51: `bash is required to load <path>`) · TXT-7 `unset` message
(**live**, #51: `<path>: configuration name '<NAME>' was unset`) · TXT-8
lib-dir line (**live**, #52: one new line, `installed -> <lib dir>`, printed by
the lib step only when the copy actually ran — issue #39 decision 15 / issue
#40 decision 13. The reference's bin-step lines are unchanged: the bin step
still prints `installed -> <bin>` or `already installed at <bin>`
character-for-character) · TXT-9 generic lib-step error (**live**, #52: one
message for the whole lib step — `[ERROR: could not install <lib dir>: <system
error>]` for a filesystem failure, and for the ownership-guard refusal the same
`could not install <lib dir>:` prefix with one fixed reason covering both the
symlink and the foreign-content case, `it is a symlink or is not an
agent-code-intel install (no agent_code_intel/__init__.py)` — issue #40
decision 14 rejects a bespoke text per case; the exact reason wording is the
spec-writer's call and is **open for a wording ruling at review**). Everything
else is character-for-character, including `die` / `say` / `row` / `hr` and the
permission-rule and PATH-advisory lines.

---

## 6. Coverage ledger

Every invariant from the [behaviour inventory (issue #34)](https://github.com/eduardtomasek/agent-code-intel/issues/34)
and the divergence list (issue #35). Each row names the scenario that will
carry it and the slice that will turn it green. A row is `green` only once its
scenario runs green in **both** relevant lanes against the candidate; it is
never reported as `skip` or as an expected pass (issue #46 §3). `partial` marks
a row whose parser-scope behaviour is proven now but whose full proof waits on
a later slice's mode (an effect only visible once something prints a header, an
exit-2 drift path, …).

**Issue #50** turns the runtime gate and the CLI parser green (rows RT-2, RT-4,
RT-6…RT-8, RT-10…RT-12, RT-14; FMT-1/FMT-2 for the `die`/`say` primitives the
parser exercises).

**Issue #51** turns config loading and identity resolution green: CFG-2…CFG-4,
CFG-6…CFG-9, CFG-11, CFG-12, ID-1, ID-3, RT-13/TOL-7 (`canon`), and DEV-3,
DEV-10, DEV-13, DEV-14. CFG-1, CFG-5, CFG-10, CFG-13, CFG-14, ID-2, ID-4, ID-5,
ID-6 and TOL-1 are `partial` — the pure logic is proven at the U layer now, the
D proof waits on a mode that prints the banner or the JSON (#53) or, for
CFG-10, is an acknowledged hermetic gap (`bash` is on every isolated PATH).
Everything else remains `unimplemented`.

**Issue #52** turns install / upgrade green: INST-1, INST-2, INST-3, INST-5,
INST-6, INST-7, INST-8, INST-9, INST-11, INST-12 and TOL-6. INST-4 (`--install`
from the installed copy) is green at the U layer (`test_install.Run.
test_from_installed_copy_says_already_installed_at_the_bin_path` — both steps
skip by their own realpath compare, `already installed at <bin>` unchanged, the
rest still runs) but has **no differential scenario**: the harness runs a fixed
candidate path, so it cannot install-then-re-run the installed copy — an
acknowledged harness limitation, not a behaviour gap. INST-10 stays a recorded
conscious loss (DEV-6). `install` wires `LoadedConfig.source` — its only new
input from #51 — to decide the config-template branch.

**Issue #53** turns the status mode green: RT-9, ID-7, ID-8, ID-9, JSON-1,
JSON-2, JSON-3, JSON-4, JSON-5, JSON-6, JSON-8, DEV-5, DEV-8, DEV-12, DEV-15,
TOL-1, TOL-2, and CFG-1 at the D layer (env lane byte-identical; toml lane
declares the one `meta.config_file` line). PATHS-1/PATHS-2/PATHS-3, JSON-7 (L
half), CFG-5/CFG-13/CFG-14 at D, RT-3/RT-13/ID-2/ID-4/ID-5/ID-6 at D and
DEV-7/DEV-10 stay `partial`: their remaining proof needs a stack-backed alias /
mapping fixture (grepai is absent from every hermetic PATH — issue #43 §3) or a
mode that still has not landed. The reference's `have python3 || die "python3
is required"` (`9406cce` :2222) has **no analogue and no scenario** — the port
*is* Python and status shells out to no `python3 -` heredoc for anything; a
conscious structural loss in the shape of DEV-6 / INST-10. `_status_json`
models the JSON's boolean fields as real `bool`s and every other field as a
`str` / `int` directly, rather than replaying the reference's plain-text
`"true"/"false"` → bool coercion (JSON-4); **approved at review** — for every
value the probes can produce (paths, `match`/`mismatch`/`unknown`,
`workspace_<ws>`, versions) the two are identical, and native typing is the
honest shape.

**Issue #54** turns the refresh mode green: REF-1, REF-3, REF-6, TOL-2 (the
`gn_status` half), TOL-3 and TOL-4. REF-2 and REF-4 are `partial` — the
git-root resolution and exit 1 (preflight / missing-`.code-intel` / outside-git)
are D green (`refresh-skip-both`, `refresh-no-code-intel`,
`refresh-preflight-blocked`, `identity-refresh-outside-git`) and B green
(`test_refresh_finds_git_root_from_subdirectory`,
`test_refresh_path_flag_is_explicit_override`), but exit 2 (drift / failed
re-index) is hermetically unreachable — reaching the audit at all needs a real
grepai+qdrant or gitnexus — so it is U-only (`test_refresh.Reindex` /
`test_refresh.Ordering`, which also pin the preflight and re-index seam-call
order). REF-5 is green at the narrow-double (U)
layer; REF-7 (full `gitnexus analyze` output, not `tail -20`)
stays L, pending post-switch live evidence. The reference's `have python3 || die "python3 is
required"` (`9406cce` :2227) has **no port analogue** — the same conscious
structural loss recorded for `--status` (DEV-6 / INST-10 shape); no scenario.
The re-index and watcher-start commands go through the same `Stack` `execute`
seam every other probe uses, and the port **captures then replays** their
output where the reference streams it live — an approved divergence in the
shape of DEV-15's bounded probes: `--refresh` re-index output is L-tested only
(REF-7) and no hermetic scenario reaches it.

`config.load` returns an immutable `Config` and `ChildEnvironment` and
`resolve_project` an immutable `ProjectContext` (criterion 5, part 1); #53 is
the first slice to wire those values *into* a mode.
The bash harvester (issue #38 §3) transfers via two files in a private,
auto-cleaned temp dir rather than "fd 3" — a deliberate, contract-equivalent
choice (NUL-safe, off stdout, env never logged) that avoids a pipe deadlock and
a clash with a `defaults.env` that itself does `exec 3>…`; noted for a ruling at
review.

Verification layer key: **B** existing black-box · **U** unittest · **D**
differential (env+toml) · **L** live.

### 6.1 Runtime and CLI parser — reference §1–3, 6

| ID | Invariant (reference cite) | Scenario | Layer | Status |
|---|---|---|---|---|
| RT-1 | fixed 15-step validation pipeline; config-source before arg-parse; install before path-validation; home guards gated on `--all` not `--json` (`:88`–`:2217`) | `test_cli_parser.Pipeline` | U | **green** (#57) — config load → parse → early install and config load → parse → project resolution → mode dispatch are pinned by call-order tests |
| RT-2 | 22 flags: arity, target var, default, repetition/conflict rule (`:262`–`:297`) | `cli-flags-matrix` + `cli-version` | B+U+D | **green** (#50) — full 22-flag matrix in `test_cli_parser.py`; D spot-checks `--agent`, seeds |
| RT-3 | last mode-switch wins; no mutual-exclusivity check (`:275`–`:278`) | `cli-mode-last-wins` | U+D | **partial** (#50) — U green (`test_last_mode_switch_wins…`); a D scenario driving `--remove --status` → status output waits on `--remove` landing (#56) |
| RT-4 | `--help`/`--version` handled in-loop; short-circuit right, not left; first wins (`:291`–`:292`) | `cli-early-exit` | U+D | **green** (#50) |
| RT-5 | exit map 0 ok / 1 error / 2 ran-with-drift; leaked child code otherwise (`:133`, §6) | *(spans every mode scenario)* | B+D | **partial** (#50) — 0 and 1 proven by the parser scenarios; 2 / leaked-child wait on the modes |
| RT-6 | unknown `-*` → `[ERROR: unknown flag: …]` stderr, exit 1 (`:293`) | `cli-unknown-flag` | B+D | **green** (#50) |
| RT-7 | second positional → `[ERROR: workspace name given twice …]` (`:295`) | `cli-workspace-twice` | B+U+D | **green** (#50) |
| RT-8 | `--agent` enum claude/codex/both, else die; missing arg dies (`:268`–`:272`) | `cli-flags-matrix` | B+U+D | **green** (#50) |
| RT-9 | `--json` suppresses the Project/Workspace/Agents header in every mode (`:2210`) — DEV-8 | `status-text-drift` / `status-json-drift` | U+D | **green** (#53) for status — U `test_status.TextTable.test_header_printed_without_json_and_suppressed_with_json`; D `status-text-drift` (header) vs `status-json-drift` (none). `--refresh` inherits it (#54, U `test_refresh.Shape.test_header_suppressed_by_json`); `--remove` at #56 |
| RT-10 | `--path` requires an argument; `ROOT` defaults to `$PWD` (`:242`, `:265`) | `cli-parse-error-exit` | B+U+D | **green** (#50) |
| RT-11 | runtime gate: `< 3.11` → approved message, exit 1, no traceback — DEV-1 / TXT-2 | `runtime-gate` | U+D | **green** (#50) — D divergence scenario + `test_runtime_gate.py` under real 3.9.6 |
| RT-12 | parse-error exit stays 1, not 2 — DEV-2 | `cli-parse-error-exit` | U+D | **green** (#50) |
| RT-13 | `canon()` prefers external `/bin/pwd`; stored identity always canonical (`:147`–`:162`) | `path-canonicalization` | U+D | **partial** (#51) — U green (`test_project.Canon`: symlinked dir resolves to the physical path, non-dir returned unchanged); the stored-identity-is-canonical D proof waits on a mode that writes `.code-intel` (#55) |
| RT-14 | `usage()` → stdout; not printed on a bad flag (§3) | `cli-early-exit` | B+U+D | **green** (#50) |

### 6.2 Config: ENV / TOML — reference §1, 7; issues #36–#38

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| CFG-1 | no config file → built-in defaults; `meta.config_file` reports `defaults.toml` (known env-lane divergence, issue #37) | `status-json-drift` | U+D | **green** (#53) — U `test_status.JsonDocument.test_config_file_reports_toml_for_a_defaults_run` (+ the env-source fallback test); D `status-json-drift` / `json-broken-project` feed both sides an equivalent config so the env lane is byte-identical and the toml lane declares only the `meta.config_file` line (`.toml` vs `.env`) via the lane-aware `scenario_expected_divergence` |
| CFG-2 | `defaults.env` only → executed as real bash from `PATH` (`:125`) | `config-env-only` | U+D | **green** (#51) — `test_config.EnvHarvester` (real bash, expansions, refs, arrays); D `config-array-empty` / `config-version-not-overridable` / `config-unset-name` all drive `defaults.env` through bash |
| CFG-3 | `defaults.toml` only → loaded via `tomllib` before arg-parse | `config-toml-only` | U+D | **green** (#51) — `test_config.TomlSchema`; D `config-toml-errors` (the candidate's `tomllib` read, both lanes) |
| CFG-4 | both files present → hard error before arg-parse (kills `--help`/`--version`) | `config-both-conflict` | D | **green** (#51) — D `config-both-conflict` (both lanes: `--version` diverges exit/stdout/stderr), U `test_config.test_both_files_is_a_hard_error_before_either_is_read` |
| CFG-5 | 13 flat TOML keys; str/int/array types; ints stringified at the external boundary | `config-toml-schema` | U+D | **partial** (#51) — U green (`test_config.test_thirteen_keys_all_accepted`: all 13, int→str, `[]`→`()`); a full 13-key D comparison waits on a mode (#53) |
| CFG-6 | 16 ENV names transferred (11 scalars + 2 arrays + CONF_DIR/CONF_FILE/REGISTRY); VERSION not among them | `config-env-transfer` | U | **green** (#51) — `test_config` (`test_conf_paths_are_transferred`, the scalar/array harvester tests) + `config-version-not-overridable` proves VERSION is excluded |
| CFG-7 | arrays replace, don't extend; empty array = valid empty list — DEV-14 | `config-array-empty` | U+D | **green** (#51) — U `test_array_replace_not_extend` / `test_empty_array_is_an_empty_list` (ENV + TOML); D `config-array-empty` |
| CFG-8 | `VERSION=` in `defaults.env` no longer overrides output — DEV-13 | `config-version-not-overridable` | D | **green** (#51) |
| CFG-9 | unknown key / wrong type / unparsable TOML → hard `[ERROR: …]` exit 1; no range/enum validation | `config-toml-errors` / `-bad-type` / `-unparsable` | U+D | **green** (#51) — U `test_config.TomlSchema` (unknown key, every wrong-type slot, unparsable passthrough, out-of-range **not** validated); D `config-toml-errors` (unknown key) + `config-toml-bad-type` + `config-toml-unparsable`, both lanes |
| CFG-10 | missing `bash` + existing `defaults.env` → `[ERROR: bash is required to load <path>]` — TXT-6 | `config-missing-bash` | D | **partial** (#51) — U green (`test_config.test_missing_bash_is_a_hard_error`); D is a hermetic gap: `bash` is on every isolated PATH (issue #43 §9) |
| CFG-11 | `unset` of a transferred name → `[ERROR: <path>: configuration name '<NAME>' was unset]` — TXT-7 | `config-unset-name` | D | **green** (#51) — D `config-unset-name` (stderr diverges from bash's own line-128 message), U `test_config.test_unset_of_a_transferred_name_is_a_hard_error` |
| CFG-12 | exported ENV delta reaches every subprocess via immutable `ChildEnvironment`; `os.environ` not mutated | `config-child-env` | U | **green** (#51) — `test_config.test_export_reaches_the_child_env_without_touching_os_environ`, `test_child_env_is_read_only` |
| CFG-13 | four preserved ENV traps (silent exit 1; `echo` corrupts JSON; inconsistent CONF_* paths; cwd-relative paths) — NOT fixed | `config-env-output` / `config-env-traps` | D | **partial** (#51) — silent-exit-1 is U green (`test_config.test_non_zero_command_kills_the_run_silently`, `CliError(wrap=False)`); the file's stdout/stderr passthrough (issue #38 §2.10, the mechanism behind the `echo`-corrupts-JSON trap) is D green in both lanes via `config-env-output`; `echo`-corrupts-JSON as observed by `code-intel-dash`, the CONF_* inconsistency and cwd-relative paths wait on the modes that expose them (#53+) |
| CFG-14 | `XDG_CONFIG_HOME` still selects config + registry location | `config-xdg-home` | B+D | **partial** (#51) — U green (`test_cli_parser.ConfDir`: `_conf_dir` honours `XDG_CONFIG_HOME`, empty falls through); D waits on a mode that reads the registry (#53) |

### 6.3 Identity and table status — reference §4, 5.5

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| ID-1 | `.code-intel` strict: allowed keys, SCHEMA, PROJECT↔dir match, duplicate key dies, never sourced | `identity-code-intel-strict` | B+U+D | **green** (#51) — U `test_project.ReadCodeIntel` (every branch, plus `test_it_never_executes_the_file`); D `identity-code-intel-broken` (unknown key → identical `[ERROR: …:line: unknown key '…']`, both lanes) |
| ID-2 | WS precedence: explicit > `.code-intel` > pristine legacy (init/ABSENT) > sanitized basename (non-refresh/ABSENT); last-resort die | `identity-precedence` | B+U+D | **partial** (#51/#53) — U green: `test_project.ResolveProject` (explicit > `.code-intel` > slug; last-resort die) and `test_project.Legacy` (`adopt_legacy_workspace` conflict rule). The explicit / `.code-intel` / basename tiers are B-proven against the candidate through `--status` (`test_code_intel_overrides_basename`, `_overrides_stale_registry`, `_explicit_arg_still_wins`); the pristine-legacy tier is #55. |
| ID-3 | `--refresh` → nearest git root; nested repo never adopts parent identity (`:522`) | `refresh-git-root` | B+D | **green** (#51) — U `test_project.test_refresh_resolves_the_nearest_git_root` (nested dir → outer root + outer WS); D `identity-refresh-outside-git` |
| ID-4 | `--path` = explicit override of the root search (`:522`) | `identity-path-override` | B+D | **partial** (#51) — U `test_project.test_refresh_with_explicit_path_skips_the_git_root_search`; D waits on a mode (#53) |
| ID-5 | `WS` sanitized, `PROJ_NAME` raw basename (`:563` vs `:565`) — DEV-10 | `identity-projname-raw` | B+U+D | **partial** (#51/#53) — U green (`test_project.test_absent_falls_back_to_the_basename_slug`); B green through the `Workspace:` banner (`test_workspace_derived_from_basename` against the candidate). The JSON `name` (raw) vs `workspace` (sanitized) contrast needs a stack-backed alias fixture (hermetic gap). |
| ID-6 | home / fs-root / parent-of-home guards; skipped only for `--status --all` (`:536`–`:541`) | `identity-home-guards` | B+U+D | **partial** (#51/#53) — U green: `test_project` covers all three guards and the `--status --all` skip; the `--status --all`-skips-guards path is exercised by every `status-all-*` D scenario (run from a subdir of the fixture, not the fixture root). A D scenario that runs `--status` from `$HOME` and asserts the die is a #55-era addition. |
| ID-7 | table rows `ok`/`DRIFT`/`BROKEN`/`GONE`/`CONFLICT`; broken row not a die in `--all`; drift criteria §5.5 | `status-all-registry` / `status-text-drift` | B+U+D | **partial** (#53) — `DRIFT`, `BROKEN`, `GONE`, `ABSENT` and the "broken row not a die" rule are D green (`status-all-registry`, both lanes) and B green (`test_status_all_continues_past_broken_code_intel`, `test_status_runs_without_the_stack`); `CONFLICT` and `ok` are U green (`test_status.TextTable`) but hermetically unreachable at D (grepai absent) — B covers `CONFLICT` via the stub (`test_grepai_name_conflict…`) |
| ID-8 | `--status --all` exit 0 all-ok incl. empty registry; exit 2 any drift | `status-all-empty` / `status-all-registry` | B+U+D | **green** (#53) — D `status-all-empty` (empty registry → `registry is empty (<path>)`, exit 0), `status-all-registry` (drift → exit 2); U `test_status` (`test_all_empty_registry_is_exit_0`, `test_no_stack_is_drift_exit_2`); B `test_status_all_tolerates_project_without_code_intel` (exit 2) |
| ID-9 | `stale .grepai/index.gob` line prints but never raises drift — DEV-12 | `status-gob-leftover` | U | **green** (#53) — U `test_status.TextTable.test_stale_gob_is_reported_but_never_the_cause_of_drift`; a D scenario needs an otherwise-healthy project, which is hermetically unreachable (grepai absent) |

### 6.4 Path alias asymmetry — issue #47

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| PATHS-1 | table prints the raw registered path; JSON prints the canonical path | `alias-path-table-vs-json` | U+D | **partial** (#53) — the code does exactly this (`_status_one` rows use the registered `path`; `_json_projects` uses `project.canon(path)`), covered by `test_status` (`test_gone_directory_is_a_gone_row` prints the raw path; `test_healthy_project_entry…` asserts `mapped_path` canonical). A table-vs-JSON D contrast on a deliberately non-canonical registered path is a follow-up scenario |
| PATHS-2 | symlinked alias → identical health from table and JSON | `test_status.TextTable.test_symlinked_alias_has_the_same_health_in_text_and_json` | U | **green** (#57) — one symlinked registry fixture proves the shared canonical health decision while preserving table raw-path / JSON canonical-path output |
| PATHS-3 | genuinely different physical dir for the same name → `CONFLICT` | `alias-conflict` | B+U | **partial** (#53) — U `test_status.TextTable.test_name_conflict_is_a_conflict_row_not_generic_drift` (mapped elsewhere → `CONFLICT` row + fix line, returns before the generic probes); B `test_grepai_name_conflict_reports_as_conflict_not_generic_drift` against the candidate (grepai stub). D is a hermetic gap |

### 6.5 JSON status contract — reference §5.6

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| JSON-1 | root keys `meta` / `tool` / `svc` / `projects`; field names, types, conditional fields per §5.6 | `status-json-drift` | B+U+D | **green** (#53) — D `status-json-drift` (env lane: full structural + raw stdout diff, zero divergence against the reference); U `test_status.JsonDocument` (root-key order, the 9-key `meta` shape, per-section key order, `chunk_size`/`grpc_port`/`project_count` are strings, `present`/`http`/`grpc`/`daemon`/`ok` are booleans, the healthy / broken / gone entry shapes and their key order); B `test_status_json_is_valid_and_has_schema`, `test_status_all_json_has_no_script_state_key` |
| JSON-2 | exit **always 0** after a successful build, even with `ok=false` everywhere — DEV-5 | `status-json-drift` | B+U+D | **green** (#53) — `run_status` returns 0 on the JSON path; D `status-json-drift` / `json-broken-project` (every probe in drift, exit 0); U `_doc` asserts exit 0; B `test_status_json_is_valid_and_has_schema` |
| JSON-3 | `--all` meaningless for the JSON branch — whole registry always enumerated | `status-json-drift` | U+D | **green** (#53) — `_status_json` never consults `status_all`; U `test_all_is_ignored_registry_always_enumerated` (`status_all=False`, both rows still enumerated); D `status-json-drift` runs `--status --all --json`, `json-broken-project` `--status --all --json` |
| JSON-4 | `"true"`/`"false"` → JSON booleans; any other value stays a string (`:1929`) | `status-json-drift` | U+D | **green** (#53) — the port types the JSON fields natively (`bool` for the flags, `str`/`int` elsewhere) instead of replaying the reference's plain-text coercion; **approved at review** (see §6 preamble), identical for every producible value. U `test_status.JsonDocument.test_tools_and_services_present_flags_are_booleans` + `test_healthy_project_entry…` (`embedder`/`collection`/`mapped_path` stay strings); D `status-json-drift` env lane (structural JSON gate, zero divergence) |
| JSON-5 | `meta.generated_at` UTC ISO-8601 `Z`; `meta.schema="1"` | `status-json-drift` | U+D | **green** (#53) — U `test_meta_shape_and_types` (`schema == "1"`, `generated_at` matches `^\d{4}-…Z$`); D `status-json-drift` gates and blanks `generated_at`, `schema` compared raw |
| JSON-6 | broken `.code-intel` → `{workspace, path, code_intel_error, ok:false}` shape | `json-broken-project` | U+D | **green** (#53) — D `json-broken-project` (env lane byte-identical: the short entry, `ok:false`, run still exits 0 with a healthy row alongside); U `test_broken_project_entry_is_the_short_shape` (exact key list) |
| JSON-7 | only `meta.config_file` and version may change vs reference; `code-intel-dash 1.1.0` unaffected | `status-json-drift` | D+L | **partial** (#53/#58) — D `status-json-drift` proves the switched candidate differs only on `script_version` in ENV and on `script_version` + `meta.config_file` in TOML; the live `code-intel-dash` consumption check is L |
| JSON-8 | JSON branch starts nothing; `mktemp` temp cleaned up (`:1771`–`:1780`) | `status-json-drift` | U+D | **green** (#53) — the port builds the document in memory, there is no `mktemp`; D `status-json-drift` / `json-broken-project` compare the file manifest and it is unchanged; U `test_nothing_is_written_to_the_project` |

### 6.6 Preview and apply — reference §5.2, 5.3

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| PREV-1 | full `preflight()` before any planning | `init-preflight-blocked` | B+D | **green** (#55) — U `test_preview_runs_preflight_and_writes_no_project_files`; D `init-preflight-blocked` passes in env/toml lanes |
| PREV-2 | preflight may start the container / `ollama serve` / pull the model; `--no-bootstrap` suppresses — DEV-4 behaviour | `init-bootstrap-effects` | L + narrow double | **partial** (#55/#59/#61) — bootstrap adapters and suppression are implemented; the three live runs were green against the existing Qdrant/Ollama stack, but no destructive bootstrap was needed on the healthy machine |
| PREV-3 | preview banner no longer claims "no changes will be made" — TXT-3 | `test_init` preview | B+D | **green** (#55) — U `test_preview_runs_preflight_and_writes_no_project_files`; the candidate black-box preview path covers the truthful banner |
| PREV-4 | preview writes nothing to the project; exit 0 / 2 / 1 | `init-preflight-blocked` / `test_init` preview | B+D | **partial** (#55/#59) — blocked preflight is differential-green and the fake-stack preview is read-only; day-1 healthy-stack apply/status/refresh evidence is recorded in §9, while all preview exit branches remain covered by the existing narrow tests |
| PREV-5 | apply's non-atomic step order 0–8 + idempotence; two hard stops after the `.code-intel` write | `test_init` apply | L + narrow double | **green for day 1** (#59) — the narrow double covers order and `.code-intel` persistence; the real checkout completed two full applies, with the second returning `Already up to date` |
| PREV-6 | legacy `refresh-intel.sh` migration: pristine adopts + deletes + strips + restarts; hand-modified refused with the exact fix command | `test_init` legacy cases | L + narrow double | **partial** (#55/#61) — migration and ownership guards are implemented; the third live lifecycle also completed the watcher restart/cleanup boundary |
| PREV-7 | `gitnexus analyze` failure → `ANALYZE_RESULT=failed` + WARNING, run continues; `--force` retry only for the one message | `run_refresh` fallback tests | narrow double + L | **partial** (#55/#61) — the first live apply emitted the known no-persisted-embeddings diagnostic and still completed successfully through the documented retry path; exact transcript remains a narrow-double concern |
| PREV-8 | `row` / `hr` formatting exact; verbs not truncated | existing formatter tests | U+D | **green** (#55) — existing formatter tests and candidate suites remain green |
| PREV-9 | `.code-intel` write not negated by `--no-git` (`:1428`) | `test_init` apply | L + narrow double | **green** (#55) — U `test_apply_keeps_mutation_order_and_writes_identity_without_git` |

### 6.7 Refresh — reference §5.4

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| REF-1 | requires an existing `.code-intel`; never founds a project (`:560`) | `refresh-no-code-intel` | B+D | **green** (#54) — D `refresh-no-code-intel` (identical `no .code-intel in <dir> …` die, nothing written, exit 1, both lanes); B `test_refresh_never_bootstraps_missing_code_intel`; U `test_project.test_refresh_never_bootstraps_an_absent_project` |
| REF-2 | root = nearest git root; `--path` overrides | `refresh-skip-both` / `refresh-no-code-intel` | B+D | **partial** (#54) — the git-root resolution is exercised at D by every refresh scenario (each runs from the fixture root = the git root) and B green (`test_refresh_finds_git_root_from_subdirectory`, `test_refresh_nested_repo_does_not_see_parent_code_intel`, `test_refresh_path_flag_is_explicit_override`); a D scenario that runs from a sub-directory / with an explicit `--path` waits on the harness gaining a per-scenario cwd |
| REF-3 | `--no-grepai` / `--no-gitnexus` each skip their side | `refresh-skip-grepai` / `refresh-skip-gitnexus` / `refresh-skip-both` | B+D | **green** (#54) — D `refresh-skip-grepai` (grepai/qdrant/ollama checks gone, `1 unmet dependency`), `refresh-skip-gitnexus` (gitnexus/git checks gone, `3 unmet dependencies`), `refresh-skip-both` (no stack needed, exit 0); B `test_refresh_no_grepai_skips_grepai_preflight` / `_no_gitnexus_` / `_with_both_stacks_skipped_needs_no_stack`; U `test_refresh.Preflight` |
| REF-4 | exit 0 fresh / 2 drift or failed re-index / 1 preflight, missing python3, missing/broken `.code-intel`, outside git | `refresh-preflight-blocked` / `refresh-skip-both` / `refresh-no-code-intel` / `identity-refresh-outside-git` | B+D | **partial** (#54) — exit 1 (preflight, missing `.code-intel`, outside git) and exit 0 (fresh) are D green in both lanes and B green; exit 2 (drift / failed re-index) is hermetically unreachable (the audit needs a real stack) — U green (`test_refresh.Reindex.test_other_analyze_failure_is_not_retried_but_audit_still_runs`, `test_stale_index_alone_is_drift_exit_2`). `missing python3` has no port analogue (DEV-6 shape) |
| REF-5 | audit still runs after a failed re-index and returns drift | `refresh-audit-after-failure` | narrow double | **green** (#54) — U `test_refresh.Reindex`: a non-retryable analyze failure (`test_other_analyze_failure_is_not_retried_but_audit_still_runs`), the `--force` retry path (`test_analyze_persisted_nothing_triggers_the_force_retry`), and a `--force` retry that *also* fails (`test_a_force_retry_that_also_fails_still_runs_the_audit`) all reach the GrepAI + GitNexus audits and return exit 2 |
| REF-6 | `not inside a git repository` message gains the real path — DEV-3 / TXT-4 | `identity-refresh-outside-git` | B+D | **green** (#54) — D `identity-refresh-outside-git` now runs against the candidate (stderr diverges: reference interpolates the blanked `ROOT`, the port the real dir); B `test_refresh_dies_outside_a_git_repository`; U `test_project.test_refresh_outside_a_git_repo_is_fatal` |
| REF-7 | full `gitnexus analyze` output (not `tail -20` like apply) | `test_refresh.Reindex.test_full_analyze_output_is_replayed_without_truncation` | U | **green** (#57) — a 25-line captured transcript is replayed from first to last line |

### 6.8 Remove — reference §5.7

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| RM-1 | dry-run builds a plan only; exit 0 nothing-to-remove / 2 actions pending | `remove-dry-run` | B+U+D | **green** (#56) — B `test_remove_dry_run_changes_nothing` + `test_remove_dry_run_reports_modified_script_as_note_not_action`; U `test_remove.test_dry_run_only_plans_and_requires_apply`; D `remove-dry-run` compares exit/stdout/stderr and the unchanged manifest in ENV and TOML lanes |
| RM-2 | apply order: stop watcher → unmap → project MCP → local files → owned blocks → optional collection → registry | `remove-apply-order` | L + narrow double | **partial** (#56) — U `test_remove.test_apply_keeps_order_and_deletes_only_owned_content` pins the full order; D `remove-apply` covers the applied teardown, while live MCP/watch verification remains environment-dependent |
| RM-3 | qdrant collection deleted only with `--purge-collection` | `remove-purge-boundary` | L | **partial** (#56) — U `test_remove.test_remote_failures_are_tolerated_and_purge_is_optional` verifies the flag boundary and exact DELETE adapter; a live Qdrant run is not part of this hermetic matrix |
| RM-4 | pristine `refresh-intel.sh` removed; hand-modified left alone with a note | `remove-legacy-script` | B+D | **green** (#56) — B `test_remove_deletes_pristine_legacy_script` + `test_remove_leaves_modified_legacy_script_alone`; U `test_remove.test_apply_keeps_order_and_deletes_only_owned_content` |
| RM-5 | every remote call `|| true` — remove works with services down | `remove-services-down` | B+U+D | **green** (#56) — B remove suite, U `test_remove.test_remote_failures_are_tolerated_and_purge_is_optional`, and D `remove-apply` pass with remote failures tolerated |
| RM-6 | user-scope `gitnexus` MCP entry kept; permission rule not removed | `remove-keeps-global-mcp` | L + narrow double | **green** (#56) — U `test_remove.test_remote_failures_are_tolerated_and_purge_is_optional` verifies only project-scoped removals are issued and the final global notice is preserved; D `remove-apply` keeps global registration state |
| RM-7 | an owned doc block that was the file's only content → whole file deleted | `remove-empties-doc` | B+D | **green** (#56) — U `test_remove.test_apply_keeps_order_and_deletes_only_owned_content` and `test_remove.test_orphaned_document_markers_are_left_untouched` cover owned-only deletion and manual/orphan protection; B remove apply coverage passes |

### 6.9 Install and upgrade — issues #39, #40; reference §5.1

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| INST-1 | U1 clean install → bin entry + lib dir; one new output line — TXT-8 | `install-clean` / `install-upgrade-v4` | B+U+D | **green** (#52) — D `install-clean` (local `stdout` diff: the one new `installed -> <lib>` line + `.env`→`.toml`; the `installed -> <bin>` line, PATH advisory and rule lines byte-identical), `install-upgrade-v4` (v4→v4 whole-tree replace); U `test_install.Run.test_clean_install_prints_every_line_in_order` |
| INST-2 | U2 v3→v4 → bash binary overwritten, lib dir created, `defaults.env` untouched, rule `already allowed` | `install-upgrade-v3` | B+U+D | **green** (#52) — D `install-upgrade-v3` (`defaults.env` hash identical after; port adds the approved `kept …` line), U `test_install` |
| INST-3 | U3 v2→v4 → also removes `code-intel-init` + both legacy rules, today's messages | `install-upgrade-v2` | B+U+D | **green** (#52) — D `install-upgrade-v2` (`removed old binary` / `removed legacy` / `allowed` byte-identical; settings.json result identical), U `test_install.Run.test_v2_upgrade…` |
| INST-4 | U4 `--install` from an installed copy → `already installed at <bin>` unchanged, both steps skipped by their own path compare, rest still runs | `install-from-installed` | U+B | **partial** (#52) — U `test_install.Run.test_from_installed_copy_says_already_installed_at_the_bin_path`; no D — the harness runs a fixed candidate path and cannot install-then-re-run the installed copy (acknowledged harness limitation) |
| INST-5 | U5 interrupted lib swap → old install untouched & working; `.new`/`.old` cleaned next run | `install-interrupted-swap` | narrow double (U) | **green** (#52) — `test_install.LibStep.test_a_failed_final_rename_restores_the_old_tree_and_reports` + `test_leftover_new_and_old_from_a_crash_are_cleaned_first` |
| INST-6 | U6 foreign content at the lib path → hard error, nothing deleted | `install-foreign-lib` | U+D | **green** (#52) — U `test_install.LibStep.test_foreign_content_at_the_lib_path_is_a_hard_error` (asserts the exact one generic message); D `install-foreign-lib` (candidate exits 1 untouched, reference oblivious) |
| INST-7 | U7 symlink at the lib path → hard error, nothing deleted | `install-symlink-lib` | U+D | **green** (#52) — U `test_install.LibStep.test_a_symlink_at_the_lib_path_is_a_hard_error`; D `install-symlink-lib` |
| INST-8 | config / PATH warning / `code-intel-init` name / exact permission rule / `--no-perms` / dashboard warning preserved | all `install-*` + `test_install_*` | B+U+D | **green** (#52) — the 7 black-box `test_install_*` pass against the candidate; the D install scenarios show the `installed -> <bin>` line, PATH advisory, `removed old binary`, `removed legacy` and `allowed` lines byte-identical; U `test_install.Run` (`--no-perms`, PATH membership, dashboard) |
| INST-9 | one generic lib-step error, path + system error — TXT-9 | `install-lib-step-error` | narrow double (U) | **green** (#52) — `test_install.LibStep.test_a_failed_copy_is_a_hard_error_naming_the_path` (`could not install <lib dir>: nope`) + the two guard tests assert the one fixed guard message |
| INST-10 | `--install` without a python3 check → 127; impossible in Python — DEV-6 conscious loss | *(recorded, not diffed)* | — | conscious loss |
| INST-11 | installed launcher's absolute shebang pins the `--install` interpreter; runs with no checkout; resists a user `PYTHONPATH` | `install-clean` + `test_install` | B+U+D | **green** (#52) — U `test_install.Run.test_installed_command_runs_standalone_from_an_unrelated_cwd` (a real subprocess: the installed launcher, run from another dir with `PATH=/usr/bin:/bin` and a `PYTHONPATH` holding a fake `agent_code_intel`, still exits 0 and prints the right version — `sys.path.insert(0, …)` wins) + `LauncherStep.test_package_path_is_relative_to_the_launcher_not_baked_absolute` (issue #39 decision 6); D `install-clean` manifest carries the launcher |
| INST-12 | lib dir replaced whole; ownership guard = not-a-symlink AND holds `agent_code_intel/__init__.py` | `install-upgrade-v4` / `install-foreign-lib` / `install-symlink-lib` | U+D | **green** (#52) — U `test_install.LibStep` (`test_replacing_an_existing_owned_install_swaps_the_whole_tree` — a stale module vanishes; the symlink / foreign guards); D `install-upgrade-v4` (the manifest diff shows `legacy_removed.py` gone on the candidate side), `install-foreign-lib`, `install-symlink-lib` |

### 6.10 Formatting primitives and tolerated failures — reference §7, 8

| ID | Invariant | Scenario | Layer | Status |
|---|---|---|---|---|
| FMT-1 | `die` → `[ERROR: %s]\n` stderr, exit 1 | `format-primitives` / `cli-*` | B+U+D | **green** (#50) — `CliError` → `[ERROR: %s]\n` + exit 1, pinned by every `cli-*` parse-error scenario and `test_cli_parser.py` |
| FMT-2 | `say` → `%s\n` stdout | `format-primitives` / `cli-version` / `status-*` | U+D | **green** (#53) — the `--version` line plus every `say` in the status table; D `status-text-drift` / `status-all-*` pin the header, the blank lines and the report tail byte-for-byte |
| FMT-3 | `row` → `  %-9s %s\n`; long verbs not truncated | `status-text-drift` / `status-all-registry` | U+D | **green** (#53) — `Reporter.row` is `"  %-9s %s\n"`; the `CONFLICT` verb (8 cols) and `BROKEN` / `DRIFT` all render at D byte-identical to the reference (`status-all-registry`), and `test_status.TextTable` asserts the exact `  DRIFT     ws  …` / `  ok        team  …` / `  BROKEN    …` / `  GONE      …` / `  CONFLICT  …` column layout |
| FMT-4 | `hr` → text + dash rule of exactly `${#1}` (UTF-8 path risk noted) | `status-text-drift` | U+D | **green** (#53) — `Reporter.hr` writes a dash rule of `len(text.encode("utf-8"))` (byte length, matching `${#1}` under the C locale the tests run in); D `status-text-drift` / `status-all-registry` render `code-intel status — <path>` (em dash = 3 bytes) and `code-intel status — all registered projects` byte-identical to the reference; U `test_hr_rule_is_byte_length_of_the_heading` |
| FMT-5 | five multi-line error blocks: `[ERROR: <sentence>]` + blank + guidance, all to stderr | `refresh-preflight-blocked` / `format-error-blocks` | B+D | **partial** (#54) — the refresh-preflight block (`[ERROR: refresh preflight found N unmet … ]` + blank + `  - <what>` / `    fix: <fix>` pairs, all on stderr) is D green (`refresh-preflight-blocked`, `refresh-skip-grepai`, `refresh-skip-gitnexus`, both lanes) and U green (`test_refresh.Preflight`); the other four blocks land with #55 |
| TOL-1 | `read_code_intel` never dies (text status); `status_one` broken/gone → row | `status-all-registry` | B+U+D | **green** (#53) — `_status_one` catches `ERR` as a `BROKEN` row and a missing dir as a `GONE` row, never raising; D `status-all-registry` (a malformed `.code-intel` mid-registry does not stop the loop), B `test_status_all_continues_past_broken_code_intel`, U `test_status.TextTable` (`test_all_reports_broken_row_and_keeps_going`, `test_gone_directory_is_a_gone_row`) |
| TOL-2 | `ws_show` / `watch_status` / `gn_status` `|| true` — absence is a finding, not an error | `status-text-drift` | U+D | **green** (#53) for the status probes — `Stack.ws_show` returns `""` and `watch_status` the merged streams on a 127, never raising; D `status-text-drift` / `status-all-*` run with no grepai and report drift rows, exit 2, no error; U `test_integrations.StackProbes` (`test_ws_show_swallows_failure`, `test_ws_exists_is_return_code`, `test_watch_status_merges_streams`). (issue #54) `gn_status` too — `Stack.gitnexus_status` returns `""` on a 127 and merges streams, U `test_integrations.StackProbes.test_gitnexus_status_merges_streams_and_runs_in_cwd`; D `refresh-skip-*` run with no gitnexus and report the preflight `MISSING` without error |
| TOL-3 | apply step 6 analyze failure tolerated; refresh re-index failure → audit continues, exit 2 | `tolerate-analyze-failure` | narrow double | **partial** (#54) — the refresh half is U green (`test_refresh.Reindex.test_other_analyze_failure_is_not_retried_but_audit_still_runs`: `_do_refresh` sets `bad`, runs both audits, returns 2); the apply-step-6 half is #55 |
| TOL-4 | `--force` retry gated on the one "persisted embeddings" message | `analyze-force-retry` | narrow double | **green** (#54) — U `test_refresh.Reindex.test_analyze_persisted_nothing_triggers_the_force_retry` (retry runs, in order, after that one message) + `test_other_analyze_failure_is_not_retried` (any other failure does **not** retry); `integrations.embeddings_not_persisted` is the gate, U `test_integrations.EmbeddingsNotPersisted` |
| TOL-5 | all `do_remove --apply` calls `|| true` | `remove-services-down` | B+U+D | **green** (#56) — B, U `test_remove.test_remote_failures_are_tolerated_and_purge_is_optional`, and D remove-apply all tolerate unavailable remote services |
| TOL-6 | `install_claude_permission` unreadable JSON → guidance + exit 0 | `install-unreadable-settings` | B+U | **green** (#52) — B `test_install_leaves_unreadable_settings_json_untouched` (candidate); U `test_install.ClaudePermission.test_unreadable_json_is_left_untouched_with_guidance` + `test_a_non_list_allow_is_left_untouched` |
| TOL-7 | `canon()` bad dir → returns input unchanged | `path-canonicalization` | U | **green** (#51) — `test_project.test_non_directory_returns_the_input_unchanged` |
| TOL-8 | old node → `warn`, not `MISSING` | `test_init.Init.test_old_node_is_a_warning_not_a_missing_dependency` | U | **green** (#57) — the preflight double exposes Node without `registerHooks` and asserts the warning vocabulary |

---

## 7. Cutover and post-switch evidence — issue #58

The switch replaces the active Bash entrypoint with the generated Python
launcher and bumps the single product-version source to 4.0.0. The mandatory
post-switch evidence commands are the following:

```
./test/unit.sh
./test/reference-suite.sh
ACI_LANES="env toml" ACI_CANDIDATE=./agent-code-intel ./test/differential.sh
```

The differential command includes the isolated install and upgrade scenarios
(`install-clean`, `install-upgrade-v2`, `install-upgrade-v3`,
`install-upgrade-v4`, `install-foreign-lib`, and `install-symlink-lib`) in both
lanes. Live stack lifecycle and candidate-SHA stamping are recorded below;
the report also states the remaining dogfooding window explicitly.

The exact switch SHA, command results, live-stack evidence and any remaining
dogfooding window are recorded below. The frozen reference remains `3.0.0`; the
candidate alone reports `4.0.0` in its banner and JSON `script_version`.

| Evidence | Result |
|---|---|
| Switching commit | `5b076e2f9816f36f251d4580f8f4567347aadef9` — active root `agent-code-intel` is the Python source launcher; `agent-code-intel.py` is removed; package version is `4.0.0` |
| Unit tests | `ACI_PYTHON=python3.11 ./test/unit.sh` — **243 passed** |
| Reference black-box | `./test/reference-suite.sh` — **53 passed, 0 failed** |
| Candidate black-box | isolated `test/run.sh` through active `agent-code-intel` — **53 passed, 0 failed** |
| Differential matrix | `ACI_LANES="env toml" ACI_CANDIDATE=./agent-code-intel ./test/differential.sh` — **72 passed, 0 failed, 0 unimplemented**; every raw and structural JSON divergence is declared by its scenario |
| Code intelligence | `./agent-code-intel --refresh` at the candidate SHA — GitNexus current/indexed commit `5b076e2`, GrepAI audit OK, exit 0 |
| Live lifecycle smoke | Isolated temporary git project, explicit Python 3.11, `--apply --agent both --no-docs` → `--status --json` (`script_version: 4.0.0`, project `ok: true`) → `--refresh` (exit 0 after the documented no-persisted-embeddings retry) → `--remove --apply --purge-collection`; real Qdrant HTTP+gRPC, Ollama model, GrepAI watcher, Claude and Codex registration all passed and were cleaned up |
| Direct source invocation under system Python 3.9 | Expected runtime-gate failure before mutation: `[ERROR: agent-code-intel requires Python 3.11 or newer (found 3.9.6)]` |
| Three-day dogfooding | **Three live sessions recorded; acceptance gate partial** — the same candidate implementation SHA was used on three real repositories, but the distinct-working-day and user-session/machine-restart evidence is still missing |

## 8. Acknowledged gaps (issue #43 §9) — do not block a green matrix

- Linux and any non-arm64 architecture (out of the map's scope).
- The full newer-Python matrix, notably the missing 3.12.
- Any bash other than the machine's system 3.2.
- Concurrent installs.
- Arbitrary process-interruption points beyond the specific install failures tested.
- ENV `export` reaching a foreign CLI is proven only as far as `ChildEnvironment`
  and the subprocess; how a given CLI version interprets a value is not asserted
  without separate evidence.
- The stack-dependent status probe outcomes — a workspace that exists / maps /
  has the configured embedder, a running watcher, a `CONFLICT` on a genuinely
  different physical directory, the table's raw-path vs JSON's canonical-path
  asymmetry (PATHS-1/2), the JSON `name`-vs-`workspace` case contrast (ID-5) —
  are covered at the U layer with a fake stack and, for `CONFLICT`, at the B
  layer with a grepai stub, but not at D: grepai is absent from every hermetic
  PATH (issue #43 §3). Their live proof is L and remains environment-dependent
  after the switch.
- `--status`'s and `--refresh`'s `have python3 || die "python3 is required"`
  (`9406cce` :2222, :2227) has no port analogue — the port is Python and neither
  mode runs a `python3 -` heredoc. A conscious structural loss in the shape of
  DEV-6 / INST-10; no scenario.
- `--refresh`'s re-index and watcher-start subprocesses (`gitnexus analyze
  --embeddings` / `--force`, `grepai watch --background`) stream their output
  live in the reference; the port runs them through the same capturing `Stack`
  `execute` seam as every other probe and replays the captured streams. The
  exact live interleave and the full `gitnexus analyze` transcript (REF-7) are
  L; no hermetic scenario reaches this code (the preflight exits 1
  first without a real stack).
- The reference's `qdrant_http_ok` runs `curl` with no `--max-time` /
  `--connect-timeout` (`9406cce` :583), so *the reference's* `--status --json`
  hangs indefinitely against a port that accepts the TCP connection but never
  answers HTTP (a hung/half-dead qdrant). The port does **not** — it bounds the
  probe (DEV-15). But the `status-json-drift` / `json-broken-project` **D**
  scenarios compare the port against the reference, so they still cannot run
  while a *broken* local qdrant holds 6333; they run clean where 6333 is
  healthy or absent (CI, and every normal dev machine).

## 9. Dogfooding day 1 — issue #59

Date: **2026-09-09** (first working day). This is the first live run against a
real repository rather than an isolated acceptance fixture.

| | Evidence |
|---|---|
| Repository | `/Users/eddy/Work/Personal/agent-code-intel`, an existing git checkout |
| Candidate implementation | `e0203aa9c4c61e4c25500bbbca8128e8fcc8b074` — fixes the Claude MCP lookup adapter and adds its regression test |
| Installation before upgrade | `~/.local/bin/agent-code-intel` reported `3.0.0`; `~/.local/lib/agent-code-intel` was absent; `~/.config/code-intel/defaults.env` existed at 302 bytes; `defaults.toml` was absent |
| Direct upgrade | `python3.11 ./agent-code-intel --install` returned exit 0, reported `4.0.0` afterwards, kept `defaults.env` and did not create `defaults.toml` |
| Environment | Python 3.11.16 · GrepAI 0.35.0 · GitNexus 1.6.4 · Node v24.14.0 with `register_hooks` · Qdrant HTTP+gRPC healthy · Ollama up with `nomic-embed-text-v2-moe` |
| Apply and idempotence | After the adapter fix, `agent-code-intel --apply --agent both --no-docs` returned exit 0 twice; the first indexed successfully and the second reported `Already up to date` |
| Status | `--status` returned exit 0 and `All good.`; `--status --json` returned exit 0 with `script_version: 4.0.0`, `config_file: defaults.env`, `watcher: true` and project `ok: true` |
| Refresh | `--refresh` returned exit 0 with `Code intelligence is fresh.`; GrepAI and GitNexus audits were both OK |
| Drift and repair | Changing only ignored `.grepai/config.yaml` from chunk size 256 to 999 produced text-status exit 2 and JSON `chunking_ok: false`; normal `--apply` returned exit 0, restored 256/25 and restarted the watcher; status then returned healthy and Qdrant remained green with 733 points |
| Remove boundary | `--remove --agent both --purge-collection` was run without `--apply`: exit 2 listed the watcher, project mapping/MCP entries, owned files/blocks and the optional Qdrant collection, followed by `Nothing was changed.` No destructive remove was run against the primary checkout |

The first two apply attempts before the fix returned exit 1 because the adapter
passed unsupported `-s user` to `claude mcp get`; direct CLI inspection showed
that the existing user-scope `gitnexus` registration was healthy and that only
the `get` invocation syntax was wrong. The corrected adapter uses the supported
`claude mcp get <name>` form, which is why both post-fix applies are green.

The initial non-escalated install attempt was blocked by the execution sandbox
before the CLI could write `~/.local/lib`; it was rerun with the required local
filesystem authorization. This environment error is not counted as a product
result. No service outage occurred during the accepted live cycle, and no
unrelated user data was removed or rewritten.

Day 1 satisfies the issue #59 evidence scope. Day 2 is recorded below and the
third live session follows it; the remaining distinct-day and user-session
restart gates are explicitly left open for the parent release decision.

## 10. Dogfooding day 2 — issue #60

Date: **2026-09-09** (second live session, recorded as dogfooding day 2). This
run proves the second real repository and the complete second lifecycle.

The candidate SHA is intentionally identical to day 1. The target was a clean
clone of a second real repository, not a hermetic fixture; the disposable clone
was used so the original checkout stayed untouched.

| | Evidence |
|---|---|
| Repository | Clean clone of `/Users/eddy/Work/Personal/hermes-stack`, source commit `d3e846bb41c691f7cf6116542c49be8026d62598`; disposable checkout `/private/tmp/aci60-hermes-stack-20260909-rerun` |
| Candidate implementation | `e0203aa9c4c61e4c25500bbbca8128e8fcc8b074` — the same candidate SHA recorded for day 1 |
| Environment | macOS 26.6.2 · Python 3.11.16 · GrepAI 0.35.0 · GitNexus 1.6.4 · Node v24.14.0 with `register_hooks` · Qdrant `grepai-qdrant` HTTP+gRPC healthy · Ollama up with `nomic-embed-text-v2-moe` · Claude and Codex available |
| Clean install and TOML | In a fresh `HOME`, `python3.11 <candidate>/agent-code-intel --install` returned exit 0, wrote the standalone launcher and package, and created `defaults.toml`; the generated template contained all 13 schema keys. After enabling all 13 supported values, `agent-code-intel --version` returned `4.0.0` and the JSON status reported `config_file: defaults.toml`. |
| TOML values and propagation | The enabled file used `qdrant_host=127.0.0.1`, `qdrant_http_port=6333`, `qdrant_port=6334`, `qdrant_container=grepai-qdrant`, `qdrant_image=qdrant/qdrant`, `qdrant_volume=grepai-qdrant-data`, `ollama_http=http://localhost:11434`, `embed_provider=ollama`, `embed_model=nomic-embed-text-v2-moe`, `chunk_size=256`, `chunk_overlap=25`, the 13-item lock-file/agent ignore list, and `gitignore_entries=[.grepai/, .gitnexus/]`. Parsing all 13 values succeeded; live preflight/JSON observed the Qdrant host/ports/container, Ollama endpoint/model, provider, chunking and ignore results. Image/volume remained on the bootstrap-only path because the existing Qdrant container was healthy. |
| Apply and idempotence | `agent-code-intel --apply --agent both --no-docs` returned exit 0 twice. The first GitNexus embedding attempt reported that no embeddings were persisted, then the candidate performed its documented structural retry successfully; no Python traceback or non-zero CLI result occurred. The second apply reported the project and watcher already present. |
| Status | Text `--status` returned exit 0 with `All good.`; JSON returned exit 0 with `script_version: 4.0.0`, `chunking_ok: true`, `watcher: true`, `embedder: match`, and project `ok: true`. |
| Refresh | `--refresh` returned exit 0 with `Code intelligence is fresh.`; GrepAI and GitNexus audits both reported healthy/current. |
| Drift and repair | Changing only ignored `.grepai/config.yaml` from `size: 256` to `size: 999` produced text-status exit 2 with `DRIFT`; JSON returned exit 0 with `chunking_ok: false`, as required by the established JSON-2 contract in §6.5 (health is carried in the document, not the process code). `--apply --agent both --no-docs` returned exit 0, restored `256/25`, restarted the watcher, and the next JSON status returned `chunking_ok: true` and `ok: true`. |
| Source-checkout independence | After moving the candidate source checkout away, the installed launcher still returned `agent-code-intel 4.0.0` and a healthy registry-wide JSON status from `/private/tmp`, outside the repository. |
| Remove boundary | `--remove --agent both --purge-collection` preview returned exit 2 without changes; `--remove --apply --agent both --purge-collection` returned exit 0, stopped the watcher, unmapped the project, removed project MCP entries and `.code-intel`/`.grepai`/`.gitnexus`, and deleted the temporary Qdrant collection. The remaining stack-managed files were confined to the disposable clone, which was discarded; the original `hermes-stack` checkout was never modified. |

No configuration loss, false-green status, or undocumented manual repair was
observed. The GitNexus no-persisted-embeddings message is an external tool
diagnostic recovered by the candidate's structural retry; it is recorded here
instead of being silently treated as a clean first pass. Day 2 therefore
closes the second-repository/live-cycle scope of issue #60; day 3 is recorded
below.

## 11. Dogfooding day 3 — issue #61

Date: **2026-09-09** (third live session, not a distinct calendar day from the
existing day-1/day-2 records). This
run used a clean clone of a third real repository and the same candidate
implementation as days 1–2. The first phase ended before the source checkout
was moved; the subsequent checks ran in a new login-shell process from outside
that checkout. This process boundary proves installed-CLI independence, but it
is not claimed as a user-session or machine restart.

| | Evidence |
|---|---|
| Repository | Clean clone of `/Users/eddy/Work/Personal/ai-dev-container`, source commit `de791f9498883165fcd023254a26b751cc4f8a82`; disposable checkout was removed after the run |
| Candidate implementation | `e0203aa9c4c61e4c25500bbbca8128e8fcc8b074` — identical to the candidate recorded for days 1–2 |
| Environment | macOS · Python 3.11.16 · healthy Qdrant container `grepai-qdrant` with HTTP+gRPC · Ollama with `nomic-embed-text-v2-moe` · GrepAI, GitNexus, Claude and Codex available |
| Clean install and TOML | In a fresh `HOME`, `python3.11 <candidate>/agent-code-intel --install` returned exit 0, wrote the standalone launcher/package and generated `defaults.toml`; the template contained all 13 schema keys. The default TOML configuration parsed successfully. |
| Apply and idempotence | `agent-code-intel --apply aci61-ai-dev-container --agent both --no-docs` returned exit 0 twice. The first run emitted the known GitNexus no-persisted-embeddings diagnostic and completed via the structural retry; the second reported the watcher already running. |
| Status | Text `--status` returned exit 0 with `All good.`; JSON returned exit 0 with `script_version: 4.0.0`, `embedder: match`, `chunking_ok: true`, `ignores_ok: true`, `watcher: true` and project `ok: true`. The same JSON remained healthy after the source checkout move. |
| Refresh | Healthy `--refresh` returned exit 0 with `Code intelligence is fresh.` both before drift and after repair; GitNexus indexed commit and current commit were both `de791f9`. |
| Drift and repair | Changing only `.grepai/config.yaml` from `size: 256` to `size: 999` produced text-status exit 2 with `DRIFT`; JSON returned exit 0 with `chunking_ok: false` and `ok: false`. `--apply --agent both --no-docs` returned exit 0, restored `256/25`, and the next JSON status returned `chunking_ok: true` and `ok: true`. |
| Process boundary / source-checkout independence | After moving the candidate source checkout away, a new login-shell process launched the installed command from the disposable run root: `agent-code-intel --version` returned `4.0.0` and registry-wide JSON status stayed healthy. No source checkout was required by the installed launcher. This is process-boundary evidence only; no user-session or machine restart was performed. |
| Dashboard smoke | Existing `code-intel-dash` reported version `1.1.0`; its unchanged SHA-256 was `6d4ad3f9fd449e698b7c3f17948eda8a2398743be1bd2e56fa87a6cafeecbe62`. `python3.11 code-intel-dash --once` returned exit 0 with dashboard version `1.1.0`, no status error, Qdrant/MCP/Ollama components `ok: true`, and the project healthy with GitNexus and GrepAI search checks `ok: true`. |
| Remove boundary | `--remove --agent both --purge-collection` preview returned exit 2 with `Nothing was changed.`; the apply returned exit 0, stopped the watcher, removed the project mapping and MCP entries, deleted `.code-intel`, `.grepai/`, `.gitnexus/` and the temporary Qdrant collection. The original `ai-dev-container` checkout was never modified. |

The run exposed no release-blocking condition, traceback, configuration loss,
false-green status or undocumented manual repair. The only non-zero result was
the intentional remove preview (exit 2); the final collection probe confirmed
the temporary collection was gone. The third live repository, repaired drift,
standalone launcher and unchanged `code-intel-dash` 1.1.0 close the third-session
evidence, but issue #61 remains partial until a distinct working day and an
actual user-session or machine restart are recorded.
